<h1><%= @place["name"] %></h1>

<% @entries.each do |entry| %>
  <p><strong><%= entry["title"] %></strong> — <%= entry["occurred_on"] %></p>
<% end %>

<a href="/entries/new?place_id=<%= @place["id"] %>">New Post</a>

<br><br>

<a href="/places">Back to Places</a>
